---
import LogoMapi from "@/assets/machupicchutravel.svg";
---

<header
  class="fixed flex items-center justify-center top-0 w-full mx-auto z-50 p-3 overflow-hidden"
>
  <nav class="max-w-6xl w-full mx-auto flex items-center justify-between">
    <a
      href="/"
      class="relative"
      aria-label="Ir al inicio - Logo de machupicchu Travel"
    >
      <LogoMapi class="w-20 lg:w-32 z-20" fill="#ffffff" />
      <div class="absolute top-0 w-full -z-10 size-48 bg-lime-700/90 blur-3xl">
      </div>
    </a>

    <a
      target="_blank"
      rel="noopener norederrer"
      aria-label="Iniciar conversación con un Agente de Machupicchu Travel"
      class="text-[12px] md:text-base rounded-full py-4 px-10 font-medium bg-lime-700/80 text-white transition duration-300 ease-in-out hover:bg-lime-800 uppercase"
      href="https://api.whatsapp.com/send?phone=51977777777"
      >Hablar con un Agente</a
    >
  </nav>
</header>

<style>
  header {
    background: transparent;
    animation: header-blur-scroll 0.3s linear both;
    animation-timeline: scroll();
    animation-range: 0 250px;
    border-bottom: 1px solid transparent;
  }
  @keyframes header-blur-scroll {
    0% {
      backdrop-filter: blur(0px);
      background: transparent;
    }

    100% {
      backdrop-filter: blur(10px);
      background: rgba(0, 0, 0, 0.4);
      border-bottom-color: transparent;
    }
  }
</style>
